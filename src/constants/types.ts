export interface NetworkConfig {
    CHAIN_NAME: string;
    TOKEN_NAME: string;
    QUERY_ENDPOINT?: string;
    LOGO: string;
    RPC_URL: string;
    EXISTENTIAL_DEPOSIT?: number;
    PREFIX?: number;
}
